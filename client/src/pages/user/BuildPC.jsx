export default function BuildPC() {
    return (
        <h1 className="text-3xl text-white">
            Orders
        </h1>

        /*
        Name (Optional)
        Purpose (optional)
        Purpose ? open featuredBuilds small screen under purpose : continue building
        CPU: choose first
        (Card contains cpu info + price + available stock)
        GPU: next (check for compatibility)
        RAM: Transfer/ speed info
        Storage
        OS: 10, 11, 11 Pro
        Cooler: Need based
        PSU: Based on estimated power needed
        Case: based on the parts chosen
        Monitor (optional)
        */
    );
}